@model Core.Models.Course

<div class="container mt-4">
    <div class="card">
        <div class="card-header">
            Delete Group
        </div>
        <div class="card-body">
            <h5 class="card-title">Are you sure you want to delete the group '@Model.Name'?</h5>
            <form asp-action="DeleteConfirmed" asp-controller="Course" asp-route-id="@Model.Id" method="post">
                <button type="submit" class="btn btn-danger">Delete</button>
                <a href="@Url.Action("Index", "Course")" class="btn btn-secondary">Cancel</a>
            </form>
        </div>
    </div>
</div>
@if (TempData["Error"] != null)
{
    <div class="col-md-12 alert alert-danger">
        <span><b>Sorry!</b> - @TempData["Error"]</span>
    </div>
} 
@* <div class="container mt-4"> *@
@*     <div class="card"> *@
@*         <div class="card-body"> *@
@*             <h3 class="card-title">Are you sure you want to delete this course?</h3> *@
@*             <div class="card-text"> *@
@*                 <strong>Name:</strong> @Model.Name *@
@*             </div> *@
@*             @using (Html.BeginForm()) *@
@*             { *@
@*                 <button type="submit" class="btn btn-danger mt-3">Delete</button> *@
@*                 <a href="@Url.Action("Index", "Course")" class="btn btn-secondary mt-3">Cancel</a> *@
@*             } *@
@*         </div> *@
@*     </div> *@
@* </div> *@
@* @if (TempData["Error"] != null) *@
@* { *@
@*     <div class="col-md-12 alert alert-danger"> *@
@*         <span><b>Sorry!</b> - @TempData["Error"]</span> *@
@*     </div> *@
@* } *@